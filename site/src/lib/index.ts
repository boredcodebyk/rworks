// place files you want to import through the `$lib` alias in this folder.

interface Link {
    id: string,
    name: string,
    dateCreated: Date,
    link: string
}

const db: Link[] = [

]


export {db};